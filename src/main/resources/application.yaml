spring:
  application:
    name: cicd
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://172.16.12.130:3306/test?characterEncoding=UTF-8&serverTimezone=UTC
    username: test
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.MariaDB106Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

